{% extends 'base.html.twig' %}

{% block title %}Nouvel Utilisateur — Art Connect{% endblock %}

{% block body %}
<style>
    .usr-header { margin-bottom:1rem; display:flex; align-items:center; justify-content:space-between; }
    .usr-header h1 { font-family:'Sora',sans-serif; font-weight:700; font-size:1.25rem; color:var(--ac-deep); display:flex; align-items:center; gap:.5rem; margin:0; }
    .usr-header h1 i { color:var(--ac-lavender); }
    .usr-header p { color:#6b7280; font-size:.82rem; margin:0; }
</style>

<div class="usr-header">
    <h1><i class="fas fa-user-plus"></i> Créer un utilisateur</h1>
    <p>Remplissez les informations du nouvel utilisateur</p>
</div>
{{ include('user/_form.html.twig') }}
{% endblock %}
