{% extends 'back/base_back.html.twig' %}

{% block title %}Nouvel Événement{% endblock %}

{% block page_title %}Nouvel Événement{% endblock %}

{% block body %}
<div class="container">
    <h1>Créer un Événement</h1>

    <div class="card">
        <div class="card-body">
            {{ include('back/evenement/_form.html.twig') }}
        </div>
    </div>

    <div class="mt-3">
        <a href="{{ path('admin_evenement_index') }}" class="btn btn-outline-secondary">Retour à la liste</a>
    </div>
</div>
{% endblock %}
