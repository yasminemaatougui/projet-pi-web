{% extends 'back/base_back.html.twig' %}

{% block title %}Éditer Événement{% endblock %}

{% block page_title %}Éditer Événement{% endblock %}

{% block body %}
<div class="container">
    <h1>Éditer l'Événement</h1>

    <div class="card">
        <div class="card-body">
            {{ include('back/evenement/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>

    <div class="mt-3">
        <a href="{{ path('admin_evenement_index') }}" class="btn btn-outline-secondary">Retour à la liste</a>
    </div>
</div>
{% endblock %}
